
---
- name: Install nginx
  hosts: all
  become: yes

  tasks:

  - name: install nginx
    apt: name=nginx state=latest

  - name: Start nginx and Enabled it on every boot
    service: name=nginx state=started enabled=yes
